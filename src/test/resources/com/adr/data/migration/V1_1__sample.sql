-- DROP TABLE PEOPLE;
CREATE TABLE PEOPLE(
    ID VARCHAR(32) NOT NULL,
    ALIAS VARCHAR(128) NOT NULL,
    NAME VARCHAR(1024) NOT NULL,
    BIRTHDAY TIMESTAMP,
    WEIGHT DOUBLE PRECISION,
    AGE INTEGER,
    ADULT BOOLEAN,
    ACTIVE BOOLEAN,
    IMAGE BYTEA,
    PRIMARY KEY (ID)
);
